# Secrets / per-machine env (API keys etc.) — never commit
secrets.env
*.secret
.env

# Opt-in env fragments are generated by installers, not versioned
.config/dots/envs.d/

# SSH — kept per-machine, never in the repo
ssh/

# Locally generated state
current/background
current/walls/local/

# OS junk
.DS_Store
*.swp
*.swo
*~

# Backups, audits, and migration snapshots
.pi/audit-*.log
.local/state/dots-migration/
